import React from "react";

const AbouteConstainer = ({
    children
}: {
    children: React.ReactNode
}) => {
    return ( 
        <div>
            {children}
        </div>
     );
}
 
export default AbouteConstainer;